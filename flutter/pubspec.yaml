name: mentiseterna
description: MentisEterna Flutter client with passkey authentication.
publish_to: "none"
version: 1.0.0+1

environment:
    sdk: ^3.5.0

dependencies:
    flutter:
        sdk: flutter
    http: ^1.2.0
    intl: ^0.20.2
    passkeys: ^2.19.0
    shared_preferences: ^2.3.0

dev_dependencies:
    flutter_test:
        sdk: flutter
    flutter_lints: ^6.0.0

flutter:
    uses-material-design: true
